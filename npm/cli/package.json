{
  "name": "@onetaskgraph/cli",
  "//": "llmlint: ignore-file[projects_stay_in_one_js_workspace] The launcher and the five carriers it names are published packages an end user installs, not development packages: .github/workflows/release.yml builds each carrier at release time and `npm pack`s it, so nothing under npm/ carries a binary here. In the JS workspace the optionalDependencies below would link five empty local directories, and `workspace:*` names no version the end user's npm can resolve — the literal is what selects the one carrier for their platform. scripts/set-version.sh writes all six numbers and `--check` refuses any other, and scripts/check-distribution-contract.sh reconciles this list against npm/platforms both ways, both under distribution-check. JSON has no comment to scope this to the optionalDependencies block.",
  "version": "0.2.21",
  "description": "Platform-selecting launcher for onetaskgraph",
  "x-exit-codes": {
    "64": "unsupported platform",
    "69": "carrier unavailable or could not execute",
    "70": "carrier terminated by signal"
  },
  "license": "MIT",
  "bin": {
    "onetaskgraph": "bin/onetaskgraph.js"
  },
  "files": [
    "bin"
  ],
  "optionalDependencies": {
    "@onetaskgraph/cli-linux-x64": "0.2.21",
    "@onetaskgraph/cli-linux-arm64": "0.2.21",
    "@onetaskgraph/cli-darwin-x64": "0.2.21",
    "@onetaskgraph/cli-darwin-arm64": "0.2.21",
    "@onetaskgraph/cli-win32-x64": "0.2.21"
  },
  "publishConfig": {
    "access": "public"
  }
}
