/* Generated by scripts/generate.ts. Do not edit. */
// Literal version keeps generated consumers pinned to this exact contract.
export const SCHEMA_BUNDLE_VERSION = 6 as const;
export namespace GeneratedCapabilities {
/**
 * One source's declared abilities.
 */
export interface Capabilities {
/**
 * Whether the source filters by label itself.
 */
filter_by_label: ("native" | "unsupported")
/**
 * Whether the source filters by status itself.
 */
filter_by_status: ("native" | "unsupported")
/**
 * The largest page the source will serve. At least 1 — a source that serves no rows
 * cannot be paged, and every implementation rejects zero where its config is read.
 */
max_page_size: number
/**
 * Whether the source can select tasks belonging to no project.
 */
orphan_tasks: ("native" | "unsupported")
/**
 * How far the source can walk project dependencies.
 */
project_dependencies: ("both-directions" | "forward-only")
/**
 * Whether the source has projects at all.
 */
projects: ("native" | "unsupported")
/**
 * Whether the source searches bodies itself.
 */
search_content: ("native" | "unsupported")
/**
 * Whether the source searches titles itself.
 */
search_title: ("native" | "unsupported")
/**
 * How far the source can walk task dependencies.
 */
task_dependencies: ("both-directions" | "forward-only")
[k: string]: any
}

}
export type Capabilities = GeneratedCapabilities.Capabilities;
export namespace GeneratedCredentialLayer {
/**
 * Which layer answered for one credential name.
 */
export type CredentialLayer = ("environment" | "secrets-file")

}
export type CredentialLayer = GeneratedCredentialLayer.CredentialLayer;
export namespace GeneratedDependencyEdge {
/**
 * A dependency between two work items.
 * 
 * An endpoint may name another source. Keeping that far id on the near item is work data
 * owned by its plugin, not an engine-side index or mirror; the engine reports it without
 * resolving or fetching the far item.
 */
export interface DependencyEdge {
/**
 * The item the edge starts at.
 */
from: (string | {
id: string
kind: ("task" | "project")
})
/**
 * What the edge means.
 */
kind: ("blocks" | "related")
/**
 * The item the edge points at.
 */
to: (string | {
id: string
kind: ("task" | "project")
})
[k: string]: any
}

}
export type DependencyEdge = GeneratedDependencyEdge.DependencyEdge;
export namespace GeneratedDependencyEndpoint {
/**
 * A dependency endpoint; legacy native-id strings decode as tasks.
 */
export type DependencyEndpoint = (string | {
id: string
kind: ("task" | "project")
})

}
export type DependencyEndpoint = GeneratedDependencyEndpoint.DependencyEndpoint;
export namespace GeneratedDependencyKind {
/**
 * What a [`DependencyEdge`] means.
 */
export type DependencyKind = ("blocks" | "related")

}
export type DependencyKind = GeneratedDependencyKind.DependencyKind;
export namespace GeneratedDirection {
/**
 * Which way a dependency query walks the graph.
 */
export type Direction = ("depends-on" | "depended-on-by")

}
export type Direction = GeneratedDirection.Direction;
export namespace GeneratedEffectiveConfig {
/**
 * Every setting this build reads, with its value and where the value came from.
 */
export interface EffectiveConfig {
secrets: SecretsReport
/**
 * Every setting, in key order.
 */
settings: Setting[]
[k: string]: any
}
/**
 * What the credentials file supplied, by name, and which layer answers for each.
 * 
 * Beside the settings because it is the same question — where does this value
 * come from — asked of the one kind of value that may never be printed. So the
 * name and the layer are reported and the value never is.
 */
export interface SecretsReport {
/**
 * The file that was looked for, whether or not it was there.
 */
path?: (string | null)
/**
 * Every name the file defines, in order.
 */
variables: ResolvedCredential[]
[k: string]: any
}
/**
 * One credential name the file supplied, and where its value comes from.
 */
export interface ResolvedCredential {
/**
 * The layer whose value a plugin would receive.
 */
resolved_from: ("environment" | "secrets-file")
/**
 * The variable's name. Never its value.
 */
variable: string
[k: string]: any
}
/**
 * One setting: what it is called, what it is set to, and where that came from.
 */
export interface Setting {
/**
 * The dotted path of the setting.
 */
key: string
/**
 * Where the value came from.
 */
origin: ({
layer: "default"
[k: string]: any
} | {
layer: "file"
/**
 * The document that set it.
 */
path: string
[k: string]: any
} | {
layer: "environment"
/**
 * The variable that set it.
 */
variable: string
[k: string]: any
} | {
/**
 * The flag that set it, as a user typed it.
 */
flag: string
layer: "flag"
[k: string]: any
})
/**
 * Its value.
 */
value: {
[k: string]: any
}
[k: string]: any
}

}
export type EffectiveConfig = GeneratedEffectiveConfig.EffectiveConfig;
export namespace GeneratedGlobalId {
/**
 * One item, qualified by the source it came from.
 * 
 * Rendered `<source>:<native>` and parsed by splitting on the **first** colon,
 * so a native id may contain colons freely.
 */
export type GlobalId = string

}
export type GlobalId = GeneratedGlobalId.GlobalId;
export namespace GeneratedHealth {
/**
 * Whether a source is answering right now.
 * 
 * # Placement is an open contract question
 * 
 * This type lives here because [`TaskSource::health`] returns it and the trait
 * lives here: placing it in `onetaskgraph-core` would make this crate depend on
 * the engine and invert the one direction the crate split exists to establish.
 * The approved contract enumerates this crate's contents exhaustively and does
 * not name `Health`, so the enumeration and the trait as written cannot both
 * stand. Compiling forces the placement below; the resolution — add it to the
 * enumeration, or redesign `health` so no such type crosses the boundary —
 * belongs to the contract's owner, not to this crate. See `AGENTS.md`.
 */
export interface Health {
/**
 * What the source said, when it said anything useful.
 */
detail?: (string | null)
/**
 * Whether the source answered.
 * 
 * A bare `bool` beside an untyped `detail` cannot say that an unreachable source
 * must explain itself, or keep "reachable with a warning" apart from "reachable";
 * an enum carrying the detail in its unreachable variant would.
 */
reachable: boolean
[k: string]: any
}

}
export type Health = GeneratedHealth.Health;
export namespace GeneratedItemKind {
/**
 * The kind of work item named by a dependency endpoint.
 */
export type ItemKind = ("task" | "project")

}
export type ItemKind = GeneratedItemKind.ItemKind;
export namespace GeneratedLabel {
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}

}
export type Label = GeneratedLabel.Label;
export namespace GeneratedOrigin {
/**
 * Where one setting's value came from.
 * 
 * This is what makes precedence provable rather than asserted: `config show`
 * renders it per setting, so a user sees the same answer a test does.
 */
export type Origin = ({
layer: "default"
[k: string]: any
} | {
layer: "file"
/**
 * The document that set it.
 */
path: string
[k: string]: any
} | {
layer: "environment"
/**
 * The variable that set it.
 */
variable: string
[k: string]: any
} | {
/**
 * The flag that set it, as a user typed it.
 */
flag: string
layer: "flag"
[k: string]: any
})

}
export type Origin = GeneratedOrigin.Origin;
export namespace GeneratedOutputFormat {
/**
 * How output is rendered.
 */
export type OutputFormat = ("text" | "json")

}
export type OutputFormat = GeneratedOutputFormat.OutputFormat;
export namespace GeneratedPageOfDependencyEdge {
/**
 * A plugin-defined resume token. The engine stores and returns one; it never
 * interprets one.
 */
export type Cursor = string

/**
 * One page of results, and where to pick up.
 */
export interface PageOfDependencyEdge {
/**
 * This page's items, in the source's stable order.
 */
items: DependencyEdge[]
/**
 * The cursor for the next page, or `None` when the walk is exhausted.
 */
next?: (Cursor | null)
[k: string]: any
}
/**
 * A dependency between two work items.
 * 
 * An endpoint may name another source. Keeping that far id on the near item is work data
 * owned by its plugin, not an engine-side index or mirror; the engine reports it without
 * resolving or fetching the far item.
 */
export interface DependencyEdge {
/**
 * The item the edge starts at.
 */
from: (string | {
id: string
kind: ("task" | "project")
})
/**
 * What the edge means.
 */
kind: ("blocks" | "related")
/**
 * The item the edge points at.
 */
to: (string | {
id: string
kind: ("task" | "project")
})
[k: string]: any
}

}
export type PageOfDependencyEdge = GeneratedPageOfDependencyEdge.PageOfDependencyEdge;
export namespace GeneratedPageOfLabel {
/**
 * A plugin-defined resume token. The engine stores and returns one; it never
 * interprets one.
 */
export type Cursor = string

/**
 * One page of results, and where to pick up.
 */
export interface PageOfLabel {
/**
 * This page's items, in the source's stable order.
 */
items: Label[]
/**
 * The cursor for the next page, or `None` when the walk is exhausted.
 */
next?: (Cursor | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}

}
export type PageOfLabel = GeneratedPageOfLabel.PageOfLabel;
export namespace GeneratedPageOfProject {
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string
/**
 * A plugin-defined resume token. The engine stores and returns one; it never
 * interprets one.
 */
export type Cursor = string

/**
 * One page of results, and where to pick up.
 */
export interface PageOfProject {
/**
 * This page's items, in the source's stable order.
 */
items: Project[]
/**
 * The cursor for the next page, or `None` when the walk is exhausted.
 */
next?: (Cursor | null)
[k: string]: any
}
/**
 * A grouping of tasks, shaped like a [`Task`] without a parent of its own.
 */
export interface Project {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the project was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id, for the same reason as on [`Task`].
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * Normalized repository origins this project concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the project by.
 */
title: string
/**
 * When the source says the project last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this project.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}

}
export type PageOfProject = GeneratedPageOfProject.PageOfProject;
export namespace GeneratedPageOfTask {
/**
 * A source's own opaque identifier for one item.
 * 
 * Deliberately unvalidated: a native id is whatever the upstream system says it
 * is, colons included. The engine parses a qualified id by splitting on the
 * *first* colon precisely so this stays true.
 */
export type NativeId = string
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string
/**
 * A plugin-defined resume token. The engine stores and returns one; it never
 * interprets one.
 */
export type Cursor = string

/**
 * One page of results, and where to pick up.
 */
export interface PageOfTask {
/**
 * This page's items, in the source's stable order.
 */
items: Task[]
/**
 * The cursor for the next page, or `None` when the walk is exhausted.
 */
next?: (Cursor | null)
[k: string]: any
}
/**
 * One unit of work as a source reports it.
 */
export interface Task {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the task was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id: a source returning a task already knows them.
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * `None` is a first-class case — an orphan task — not an edge case.
 */
project?: (NativeId | null)
/**
 * Normalized repository origins this task concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the task by.
 */
title: string
/**
 * When the source says the task last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this task.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}

}
export type PageOfTask = GeneratedPageOfTask.PageOfTask;
export namespace GeneratedPageRequest {
/**
 * A plugin-defined resume token. The engine stores and returns one; it never
 * interprets one.
 */
export type Cursor = string

/**
 * One step of a walk through a result set.
 */
export interface PageRequest {
/**
 * Where to resume, or `None` to start at the beginning.
 */
cursor?: (Cursor | null)
/**
 * The most items to return, at least 1. A source may return fewer, never more.
 */
limit: number
[k: string]: any
}

}
export type PageRequest = GeneratedPageRequest.PageRequest;
export namespace GeneratedPageToken {
/**
 * The engine's own resume token: one plugin cursor per source stream, opaque to the
 * caller exactly as a plugin's cursor is opaque to the engine.
 * 
 * Rendered as lower-case hex, which is not obfuscation — the inside is not a secret —
 * but the one property a token a person copies off a terminal has to have: it survives
 * a shell. The document underneath holds a plugin's own cursor, and a cursor may hold
 * anything at all, so a token spelled as the raw JSON would carry quotes, braces and
 * spaces straight into the next command line. Hex has no character a shell reads.
 * 
 * # What a token is and is not checked for
 * 
 * Both ways in go through [`parse`](Self::parse) — including deserialising one — and
 * what that establishes is **structural**: the string is hex, the bytes are this
 * engine's own resume document, and every state in it is well formed. It does not, and
 * cannot, establish that this engine is the one that wrote it. A token is not a
 * credential and carries nothing secret; forging one buys a caller nothing they could
 * not have asked for outright, since every cursor inside is handed straight back to the
 * source that issued it and is validated there.
 * 
 * What a forged token *could* do is name a stream this configuration has no source for,
 * or resume further into a page than the engine ever pages. Both are refused where the
 * token meets the query it is resuming, by
 * [`Engine`](crate::Engine) — see `EngineError::Token` — because only the engine knows
 * which sources are configured and what page ceiling each declares.
 */
export type PageToken = string

}
export type PageToken = GeneratedPageToken.PageToken;
export namespace GeneratedPredicate {
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

}
export type Predicate = GeneratedPredicate.Predicate;
export namespace GeneratedProject {
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string

/**
 * A grouping of tasks, shaped like a [`Task`] without a parent of its own.
 */
export interface Project {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the project was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id, for the same reason as on [`Task`].
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * Normalized repository origins this project concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the project by.
 */
title: string
/**
 * When the source says the project last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this project.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}

}
export type Project = GeneratedProject.Project;
export namespace GeneratedProjectQuery {
/**
 * The normalised status vocabulary shared across every source.
 */
export type StatusCategory = ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")

/**
 * A filter over a source's projects.
 */
export interface ProjectQuery {
labels: LabelFilter
/**
 * Status categories to keep. Empty means unfiltered.
 */
statuses: StatusCategory[]
/**
 * Free-text search, when the caller asked for one.
 */
text?: (TextQuery | null)
[k: string]: any
}
/**
 * Label membership.
 */
export interface LabelFilter {
/**
 * Keep an item carrying all of these.
 */
all_of: string[]
/**
 * Keep an item carrying at least one of these.
 */
any_of: string[]
/**
 * Drop an item carrying any of these.
 */
none_of: string[]
[k: string]: any
}
/**
 * A free-text search and the fields it searches.
 */
export interface TextQuery {
/**
 * Where to look for it.
 */
fields: ("title" | "content" | "title-or-content")
/**
 * What the user typed.
 */
terms: string
[k: string]: any
}

}
export type ProjectQuery = GeneratedProjectQuery.ProjectQuery;
export namespace GeneratedQualifiedEdge {
/**
 * One dependency edge with both ends qualified.
 * 
 * An end may belong to another source. The near plugin owns that qualified far id and the
 * engine reports it without resolving or fetching the far source, so it holds no index.
 */
export interface QualifiedEdge {
from: QualifiedEndpoint
/**
 * What the edge means.
 */
kind: ("blocks" | "related")
to: QualifiedEndpoint1
[k: string]: any
}
/**
 * The item the edge starts at.
 */
export interface QualifiedEndpoint {
/**
 * `<source>:<native>`, preserved when a plugin reports another source.
 */
id: string
/**
 * Whether this endpoint names a task or project.
 */
kind: ("task" | "project")
[k: string]: any
}
/**
 * The item the edge points at.
 */
export interface QualifiedEndpoint1 {
/**
 * `<source>:<native>`, preserved when a plugin reports another source.
 */
id: string
/**
 * Whether this endpoint names a task or project.
 */
kind: ("task" | "project")
[k: string]: any
}

}
export type QualifiedEdge = GeneratedQualifiedEdge.QualifiedEdge;
export namespace GeneratedQualifiedEndpoint {
/**
 * One typed, qualified endpoint in an engine dependency response.
 */
export interface QualifiedEndpoint {
/**
 * `<source>:<native>`, preserved when a plugin reports another source.
 */
id: string
/**
 * Whether this endpoint names a task or project.
 */
kind: ("task" | "project")
[k: string]: any
}

}
export type QualifiedEndpoint = GeneratedQualifiedEndpoint.QualifiedEndpoint;
export namespace GeneratedQualifiedLabel {
/**
 * One item, under the qualified id the engine addresses it by.
 * 
 * A plugin only ever deals in its own [`NativeId`]; qualifying one is the engine's job,
 * so this type is the engine's and a plugin never constructs one.
 */
export interface QualifiedLabel {
/**
 * `<source>:<native>`, the form a user types back at the command line.
 */
id: string
item: Label
[k: string]: any
}
/**
 * The item as its source reported it, unchanged.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}

}
export type QualifiedLabel = GeneratedQualifiedLabel.QualifiedLabel;
export namespace GeneratedQualifiedProject {
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string

/**
 * One item, under the qualified id the engine addresses it by.
 * 
 * A plugin only ever deals in its own [`NativeId`]; qualifying one is the engine's job,
 * so this type is the engine's and a plugin never constructs one.
 */
export interface QualifiedProject {
/**
 * `<source>:<native>`, the form a user types back at the command line.
 */
id: string
item: Project
[k: string]: any
}
/**
 * The item as its source reported it, unchanged.
 */
export interface Project {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the project was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id, for the same reason as on [`Task`].
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * Normalized repository origins this project concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the project by.
 */
title: string
/**
 * When the source says the project last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this project.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}

}
export type QualifiedProject = GeneratedQualifiedProject.QualifiedProject;
export namespace GeneratedQualifiedTask {
/**
 * A source's own opaque identifier for one item.
 * 
 * Deliberately unvalidated: a native id is whatever the upstream system says it
 * is, colons included. The engine parses a qualified id by splitting on the
 * *first* colon precisely so this stays true.
 */
export type NativeId = string
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string

/**
 * One item, under the qualified id the engine addresses it by.
 * 
 * A plugin only ever deals in its own [`NativeId`]; qualifying one is the engine's job,
 * so this type is the engine's and a plugin never constructs one.
 */
export interface QualifiedTask {
/**
 * `<source>:<native>`, the form a user types back at the command line.
 */
id: string
item: Task
[k: string]: any
}
/**
 * The item as its source reported it, unchanged.
 */
export interface Task {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the task was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id: a source returning a task already knows them.
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * `None` is a first-class case — an orphan task — not an edge case.
 */
project?: (NativeId | null)
/**
 * Normalized repository origins this task concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the task by.
 */
title: string
/**
 * When the source says the task last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this task.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}

}
export type QualifiedTask = GeneratedQualifiedTask.QualifiedTask;
export namespace GeneratedQueryPlan {
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

/**
 * What the engine did, per source.
 */
export interface QueryPlan {
/**
 * One entry per source the query reached.
 */
per_source: SourcePlan[]
[k: string]: any
}
/**
 * What one source was asked for, and what happened to each predicate.
 */
export interface SourcePlan {
/**
 * Predicates the engine applied in memory over a wider result set.
 */
applied_locally: Predicate[]
/**
 * Predicates the engine answered by a bounded scan of the source.
 */
emulated: Predicate[]
/**
 * The plugin kind behind it.
 */
kind: string
/**
 * How many pages the engine pulled from this source to answer.
 */
pages_fetched: number
/**
 * Predicates the source applied itself.
 * 
 * The four predicate vectors below partition one set of outcomes, and nothing in
 * the type says so: a `Predicate` could appear in two of them at once, or in none.
 * One `Vec<(Predicate, Outcome)>` — or a map keyed by predicate — would make that
 * unrepresentable. See the directive below for why it stays as it is.
 */
pushed_down: Predicate[]
/**
 * The configured source this describes.
 */
source: string
/**
 * Predicates neither side could answer, so the result is unconstrained.
 * 
 * Never [`Predicate::ReverseDependencies`]: `DependencySupport` has no
 * unsupported variant, so a reverse-dependency read is answered natively or
 * emulated by the engine's bounded scan, never abandoned. The type cannot say
 * so — see the directive below.
 */
unavailable: Predicate[]
[k: string]: any
}

}
export type QueryPlan = GeneratedQueryPlan.QueryPlan;
export namespace GeneratedQueryResponseOfQualifiedEdge {
/**
 * The engine's own resume token: one plugin cursor per source stream, opaque to the
 * caller exactly as a plugin's cursor is opaque to the engine.
 * 
 * Rendered as lower-case hex, which is not obfuscation — the inside is not a secret —
 * but the one property a token a person copies off a terminal has to have: it survives
 * a shell. The document underneath holds a plugin's own cursor, and a cursor may hold
 * anything at all, so a token spelled as the raw JSON would carry quotes, braces and
 * spaces straight into the next command line. Hex has no character a shell reads.
 * 
 * # What a token is and is not checked for
 * 
 * Both ways in go through [`parse`](Self::parse) — including deserialising one — and
 * what that establishes is **structural**: the string is hex, the bytes are this
 * engine's own resume document, and every state in it is well formed. It does not, and
 * cannot, establish that this engine is the one that wrote it. A token is not a
 * credential and carries nothing secret; forging one buys a caller nothing they could
 * not have asked for outright, since every cursor inside is handed straight back to the
 * source that issued it and is validated there.
 * 
 * What a forged token *could* do is name a stream this configuration has no source for,
 * or resume further into a page than the engine ever pages. Both are refused where the
 * token meets the query it is resuming, by
 * [`Engine`](crate::Engine) — see `EngineError::Token` — because only the engine knows
 * which sources are configured and what page ceiling each declares.
 */
export type PageToken = string
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

/**
 * One page of engine output, with the plan that produced it.
 */
export interface QueryResponseOfQualifiedEdge {
/**
 * Sources that failed. One failure never fails the whole query.
 */
errors: SourceFailure[]
/**
 * This page's items, already qualified and merged across sources.
 */
items: QualifiedEdge[]
/**
 * Where to resume, or `None` when every source is exhausted.
 */
next?: (PageToken | null)
plan: QueryPlan
[k: string]: any
}
/**
 * One source's failure, kept beside the results the other sources returned.
 */
export interface SourceFailure {
/**
 * Why.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
/**
 * The source that failed.
 */
source: string
[k: string]: any
}
/**
 * One dependency edge with both ends qualified.
 * 
 * An end may belong to another source. The near plugin owns that qualified far id and the
 * engine reports it without resolving or fetching the far source, so it holds no index.
 */
export interface QualifiedEdge {
from: QualifiedEndpoint
/**
 * What the edge means.
 */
kind: ("blocks" | "related")
to: QualifiedEndpoint1
[k: string]: any
}
/**
 * The item the edge starts at.
 */
export interface QualifiedEndpoint {
/**
 * `<source>:<native>`, preserved when a plugin reports another source.
 */
id: string
/**
 * Whether this endpoint names a task or project.
 */
kind: ("task" | "project")
[k: string]: any
}
/**
 * The item the edge points at.
 */
export interface QualifiedEndpoint1 {
/**
 * `<source>:<native>`, preserved when a plugin reports another source.
 */
id: string
/**
 * Whether this endpoint names a task or project.
 */
kind: ("task" | "project")
[k: string]: any
}
/**
 * What each source was asked to do, and what the engine did instead.
 */
export interface QueryPlan {
/**
 * One entry per source the query reached.
 */
per_source: SourcePlan[]
[k: string]: any
}
/**
 * What one source was asked for, and what happened to each predicate.
 */
export interface SourcePlan {
/**
 * Predicates the engine applied in memory over a wider result set.
 */
applied_locally: Predicate[]
/**
 * Predicates the engine answered by a bounded scan of the source.
 */
emulated: Predicate[]
/**
 * The plugin kind behind it.
 */
kind: string
/**
 * How many pages the engine pulled from this source to answer.
 */
pages_fetched: number
/**
 * Predicates the source applied itself.
 * 
 * The four predicate vectors below partition one set of outcomes, and nothing in
 * the type says so: a `Predicate` could appear in two of them at once, or in none.
 * One `Vec<(Predicate, Outcome)>` — or a map keyed by predicate — would make that
 * unrepresentable. See the directive below for why it stays as it is.
 */
pushed_down: Predicate[]
/**
 * The configured source this describes.
 */
source: string
/**
 * Predicates neither side could answer, so the result is unconstrained.
 * 
 * Never [`Predicate::ReverseDependencies`]: `DependencySupport` has no
 * unsupported variant, so a reverse-dependency read is answered natively or
 * emulated by the engine's bounded scan, never abandoned. The type cannot say
 * so — see the directive below.
 */
unavailable: Predicate[]
[k: string]: any
}

}
export type QueryResponseOfQualifiedEdge = GeneratedQueryResponseOfQualifiedEdge.QueryResponseOfQualifiedEdge;
export namespace GeneratedQueryResponseOfQualifiedLabel {
/**
 * The engine's own resume token: one plugin cursor per source stream, opaque to the
 * caller exactly as a plugin's cursor is opaque to the engine.
 * 
 * Rendered as lower-case hex, which is not obfuscation — the inside is not a secret —
 * but the one property a token a person copies off a terminal has to have: it survives
 * a shell. The document underneath holds a plugin's own cursor, and a cursor may hold
 * anything at all, so a token spelled as the raw JSON would carry quotes, braces and
 * spaces straight into the next command line. Hex has no character a shell reads.
 * 
 * # What a token is and is not checked for
 * 
 * Both ways in go through [`parse`](Self::parse) — including deserialising one — and
 * what that establishes is **structural**: the string is hex, the bytes are this
 * engine's own resume document, and every state in it is well formed. It does not, and
 * cannot, establish that this engine is the one that wrote it. A token is not a
 * credential and carries nothing secret; forging one buys a caller nothing they could
 * not have asked for outright, since every cursor inside is handed straight back to the
 * source that issued it and is validated there.
 * 
 * What a forged token *could* do is name a stream this configuration has no source for,
 * or resume further into a page than the engine ever pages. Both are refused where the
 * token meets the query it is resuming, by
 * [`Engine`](crate::Engine) — see `EngineError::Token` — because only the engine knows
 * which sources are configured and what page ceiling each declares.
 */
export type PageToken = string
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

/**
 * One page of engine output, with the plan that produced it.
 */
export interface QueryResponseOfQualifiedLabel {
/**
 * Sources that failed. One failure never fails the whole query.
 */
errors: SourceFailure[]
/**
 * This page's items, already qualified and merged across sources.
 */
items: Qualified[]
/**
 * Where to resume, or `None` when every source is exhausted.
 */
next?: (PageToken | null)
plan: QueryPlan
[k: string]: any
}
/**
 * One source's failure, kept beside the results the other sources returned.
 */
export interface SourceFailure {
/**
 * Why.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
/**
 * The source that failed.
 */
source: string
[k: string]: any
}
/**
 * One item, under the qualified id the engine addresses it by.
 * 
 * A plugin only ever deals in its own [`NativeId`]; qualifying one is the engine's job,
 * so this type is the engine's and a plugin never constructs one.
 */
export interface Qualified {
/**
 * `<source>:<native>`, the form a user types back at the command line.
 */
id: string
item: Label
[k: string]: any
}
/**
 * The item as its source reported it, unchanged.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * What each source was asked to do, and what the engine did instead.
 */
export interface QueryPlan {
/**
 * One entry per source the query reached.
 */
per_source: SourcePlan[]
[k: string]: any
}
/**
 * What one source was asked for, and what happened to each predicate.
 */
export interface SourcePlan {
/**
 * Predicates the engine applied in memory over a wider result set.
 */
applied_locally: Predicate[]
/**
 * Predicates the engine answered by a bounded scan of the source.
 */
emulated: Predicate[]
/**
 * The plugin kind behind it.
 */
kind: string
/**
 * How many pages the engine pulled from this source to answer.
 */
pages_fetched: number
/**
 * Predicates the source applied itself.
 * 
 * The four predicate vectors below partition one set of outcomes, and nothing in
 * the type says so: a `Predicate` could appear in two of them at once, or in none.
 * One `Vec<(Predicate, Outcome)>` — or a map keyed by predicate — would make that
 * unrepresentable. See the directive below for why it stays as it is.
 */
pushed_down: Predicate[]
/**
 * The configured source this describes.
 */
source: string
/**
 * Predicates neither side could answer, so the result is unconstrained.
 * 
 * Never [`Predicate::ReverseDependencies`]: `DependencySupport` has no
 * unsupported variant, so a reverse-dependency read is answered natively or
 * emulated by the engine's bounded scan, never abandoned. The type cannot say
 * so — see the directive below.
 */
unavailable: Predicate[]
[k: string]: any
}

}
export type QueryResponseOfQualifiedLabel = GeneratedQueryResponseOfQualifiedLabel.QueryResponseOfQualifiedLabel;
export namespace GeneratedQueryResponseOfQualifiedProject {
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string
/**
 * The engine's own resume token: one plugin cursor per source stream, opaque to the
 * caller exactly as a plugin's cursor is opaque to the engine.
 * 
 * Rendered as lower-case hex, which is not obfuscation — the inside is not a secret —
 * but the one property a token a person copies off a terminal has to have: it survives
 * a shell. The document underneath holds a plugin's own cursor, and a cursor may hold
 * anything at all, so a token spelled as the raw JSON would carry quotes, braces and
 * spaces straight into the next command line. Hex has no character a shell reads.
 * 
 * # What a token is and is not checked for
 * 
 * Both ways in go through [`parse`](Self::parse) — including deserialising one — and
 * what that establishes is **structural**: the string is hex, the bytes are this
 * engine's own resume document, and every state in it is well formed. It does not, and
 * cannot, establish that this engine is the one that wrote it. A token is not a
 * credential and carries nothing secret; forging one buys a caller nothing they could
 * not have asked for outright, since every cursor inside is handed straight back to the
 * source that issued it and is validated there.
 * 
 * What a forged token *could* do is name a stream this configuration has no source for,
 * or resume further into a page than the engine ever pages. Both are refused where the
 * token meets the query it is resuming, by
 * [`Engine`](crate::Engine) — see `EngineError::Token` — because only the engine knows
 * which sources are configured and what page ceiling each declares.
 */
export type PageToken = string
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

/**
 * One page of engine output, with the plan that produced it.
 */
export interface QueryResponseOfQualifiedProject {
/**
 * Sources that failed. One failure never fails the whole query.
 */
errors: SourceFailure[]
/**
 * This page's items, already qualified and merged across sources.
 */
items: Qualified[]
/**
 * Where to resume, or `None` when every source is exhausted.
 */
next?: (PageToken | null)
plan: QueryPlan
[k: string]: any
}
/**
 * One source's failure, kept beside the results the other sources returned.
 */
export interface SourceFailure {
/**
 * Why.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
/**
 * The source that failed.
 */
source: string
[k: string]: any
}
/**
 * One item, under the qualified id the engine addresses it by.
 * 
 * A plugin only ever deals in its own [`NativeId`]; qualifying one is the engine's job,
 * so this type is the engine's and a plugin never constructs one.
 */
export interface Qualified {
/**
 * `<source>:<native>`, the form a user types back at the command line.
 */
id: string
item: Project
[k: string]: any
}
/**
 * The item as its source reported it, unchanged.
 */
export interface Project {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the project was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id, for the same reason as on [`Task`].
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * Normalized repository origins this project concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the project by.
 */
title: string
/**
 * When the source says the project last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this project.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}
/**
 * What each source was asked to do, and what the engine did instead.
 */
export interface QueryPlan {
/**
 * One entry per source the query reached.
 */
per_source: SourcePlan[]
[k: string]: any
}
/**
 * What one source was asked for, and what happened to each predicate.
 */
export interface SourcePlan {
/**
 * Predicates the engine applied in memory over a wider result set.
 */
applied_locally: Predicate[]
/**
 * Predicates the engine answered by a bounded scan of the source.
 */
emulated: Predicate[]
/**
 * The plugin kind behind it.
 */
kind: string
/**
 * How many pages the engine pulled from this source to answer.
 */
pages_fetched: number
/**
 * Predicates the source applied itself.
 * 
 * The four predicate vectors below partition one set of outcomes, and nothing in
 * the type says so: a `Predicate` could appear in two of them at once, or in none.
 * One `Vec<(Predicate, Outcome)>` — or a map keyed by predicate — would make that
 * unrepresentable. See the directive below for why it stays as it is.
 */
pushed_down: Predicate[]
/**
 * The configured source this describes.
 */
source: string
/**
 * Predicates neither side could answer, so the result is unconstrained.
 * 
 * Never [`Predicate::ReverseDependencies`]: `DependencySupport` has no
 * unsupported variant, so a reverse-dependency read is answered natively or
 * emulated by the engine's bounded scan, never abandoned. The type cannot say
 * so — see the directive below.
 */
unavailable: Predicate[]
[k: string]: any
}

}
export type QueryResponseOfQualifiedProject = GeneratedQueryResponseOfQualifiedProject.QueryResponseOfQualifiedProject;
export namespace GeneratedQueryResponseOfQualifiedTask {
/**
 * A source's own opaque identifier for one item.
 * 
 * Deliberately unvalidated: a native id is whatever the upstream system says it
 * is, colons included. The engine parses a qualified id by splitting on the
 * *first* colon precisely so this stays true.
 */
export type NativeId = string
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string
/**
 * The engine's own resume token: one plugin cursor per source stream, opaque to the
 * caller exactly as a plugin's cursor is opaque to the engine.
 * 
 * Rendered as lower-case hex, which is not obfuscation — the inside is not a secret —
 * but the one property a token a person copies off a terminal has to have: it survives
 * a shell. The document underneath holds a plugin's own cursor, and a cursor may hold
 * anything at all, so a token spelled as the raw JSON would carry quotes, braces and
 * spaces straight into the next command line. Hex has no character a shell reads.
 * 
 * # What a token is and is not checked for
 * 
 * Both ways in go through [`parse`](Self::parse) — including deserialising one — and
 * what that establishes is **structural**: the string is hex, the bytes are this
 * engine's own resume document, and every state in it is well formed. It does not, and
 * cannot, establish that this engine is the one that wrote it. A token is not a
 * credential and carries nothing secret; forging one buys a caller nothing they could
 * not have asked for outright, since every cursor inside is handed straight back to the
 * source that issued it and is validated there.
 * 
 * What a forged token *could* do is name a stream this configuration has no source for,
 * or resume further into a page than the engine ever pages. Both are refused where the
 * token meets the query it is resuming, by
 * [`Engine`](crate::Engine) — see `EngineError::Token` — because only the engine knows
 * which sources are configured and what page ceiling each declares.
 */
export type PageToken = string
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

/**
 * One page of engine output, with the plan that produced it.
 */
export interface QueryResponseOfQualifiedTask {
/**
 * Sources that failed. One failure never fails the whole query.
 */
errors: SourceFailure[]
/**
 * This page's items, already qualified and merged across sources.
 */
items: Qualified[]
/**
 * Where to resume, or `None` when every source is exhausted.
 */
next?: (PageToken | null)
plan: QueryPlan
[k: string]: any
}
/**
 * One source's failure, kept beside the results the other sources returned.
 */
export interface SourceFailure {
/**
 * Why.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
/**
 * The source that failed.
 */
source: string
[k: string]: any
}
/**
 * One item, under the qualified id the engine addresses it by.
 * 
 * A plugin only ever deals in its own [`NativeId`]; qualifying one is the engine's job,
 * so this type is the engine's and a plugin never constructs one.
 */
export interface Qualified {
/**
 * `<source>:<native>`, the form a user types back at the command line.
 */
id: string
item: Task
[k: string]: any
}
/**
 * The item as its source reported it, unchanged.
 */
export interface Task {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the task was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id: a source returning a task already knows them.
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * `None` is a first-class case — an orphan task — not an edge case.
 */
project?: (NativeId | null)
/**
 * Normalized repository origins this task concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the task by.
 */
title: string
/**
 * When the source says the task last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this task.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}
/**
 * What each source was asked to do, and what the engine did instead.
 */
export interface QueryPlan {
/**
 * One entry per source the query reached.
 */
per_source: SourcePlan[]
[k: string]: any
}
/**
 * What one source was asked for, and what happened to each predicate.
 */
export interface SourcePlan {
/**
 * Predicates the engine applied in memory over a wider result set.
 */
applied_locally: Predicate[]
/**
 * Predicates the engine answered by a bounded scan of the source.
 */
emulated: Predicate[]
/**
 * The plugin kind behind it.
 */
kind: string
/**
 * How many pages the engine pulled from this source to answer.
 */
pages_fetched: number
/**
 * Predicates the source applied itself.
 * 
 * The four predicate vectors below partition one set of outcomes, and nothing in
 * the type says so: a `Predicate` could appear in two of them at once, or in none.
 * One `Vec<(Predicate, Outcome)>` — or a map keyed by predicate — would make that
 * unrepresentable. See the directive below for why it stays as it is.
 */
pushed_down: Predicate[]
/**
 * The configured source this describes.
 */
source: string
/**
 * Predicates neither side could answer, so the result is unconstrained.
 * 
 * Never [`Predicate::ReverseDependencies`]: `DependencySupport` has no
 * unsupported variant, so a reverse-dependency read is answered natively or
 * emulated by the engine's bounded scan, never abandoned. The type cannot say
 * so — see the directive below.
 */
unavailable: Predicate[]
[k: string]: any
}

}
export type QueryResponseOfQualifiedTask = GeneratedQueryResponseOfQualifiedTask.QueryResponseOfQualifiedTask;
export namespace GeneratedQueryResponseOfSearchHit {
/**
 * One hit of a search that may cross entities.
 */
export type SearchHit = (Qualified | Qualified2)
/**
 * The engine's own resume token: one plugin cursor per source stream, opaque to the
 * caller exactly as a plugin's cursor is opaque to the engine.
 * 
 * Rendered as lower-case hex, which is not obfuscation — the inside is not a secret —
 * but the one property a token a person copies off a terminal has to have: it survives
 * a shell. The document underneath holds a plugin's own cursor, and a cursor may hold
 * anything at all, so a token spelled as the raw JSON would carry quotes, braces and
 * spaces straight into the next command line. Hex has no character a shell reads.
 * 
 * # What a token is and is not checked for
 * 
 * Both ways in go through [`parse`](Self::parse) — including deserialising one — and
 * what that establishes is **structural**: the string is hex, the bytes are this
 * engine's own resume document, and every state in it is well formed. It does not, and
 * cannot, establish that this engine is the one that wrote it. A token is not a
 * credential and carries nothing secret; forging one buys a caller nothing they could
 * not have asked for outright, since every cursor inside is handed straight back to the
 * source that issued it and is validated there.
 * 
 * What a forged token *could* do is name a stream this configuration has no source for,
 * or resume further into a page than the engine ever pages. Both are refused where the
 * token meets the query it is resuming, by
 * [`Engine`](crate::Engine) — see `EngineError::Token` — because only the engine knows
 * which sources are configured and what page ceiling each declares.
 */
export type PageToken = string
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

/**
 * One page of engine output, with the plan that produced it.
 */
export interface QueryResponseOfSearchHit {
/**
 * Sources that failed. One failure never fails the whole query.
 */
errors: SourceFailure[]
/**
 * This page's items, already qualified and merged across sources.
 */
items: SearchHit[]
/**
 * Where to resume, or `None` when every source is exhausted.
 */
next?: (PageToken | null)
plan: QueryPlan
[k: string]: any
}
/**
 * One source's failure, kept beside the results the other sources returned.
 */
export interface SourceFailure {
/**
 * Why.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
/**
 * The source that failed.
 */
source: string
[k: string]: any
}
/**
 * A task matched.
 */
export interface Qualified {
kind: "task"
[k: string]: any
}
/**
 * A project matched.
 */
export interface Qualified2 {
kind: "project"
[k: string]: any
}
/**
 * What each source was asked to do, and what the engine did instead.
 */
export interface QueryPlan {
/**
 * One entry per source the query reached.
 */
per_source: SourcePlan[]
[k: string]: any
}
/**
 * What one source was asked for, and what happened to each predicate.
 */
export interface SourcePlan {
/**
 * Predicates the engine applied in memory over a wider result set.
 */
applied_locally: Predicate[]
/**
 * Predicates the engine answered by a bounded scan of the source.
 */
emulated: Predicate[]
/**
 * The plugin kind behind it.
 */
kind: string
/**
 * How many pages the engine pulled from this source to answer.
 */
pages_fetched: number
/**
 * Predicates the source applied itself.
 * 
 * The four predicate vectors below partition one set of outcomes, and nothing in
 * the type says so: a `Predicate` could appear in two of them at once, or in none.
 * One `Vec<(Predicate, Outcome)>` — or a map keyed by predicate — would make that
 * unrepresentable. See the directive below for why it stays as it is.
 */
pushed_down: Predicate[]
/**
 * The configured source this describes.
 */
source: string
/**
 * Predicates neither side could answer, so the result is unconstrained.
 * 
 * Never [`Predicate::ReverseDependencies`]: `DependencySupport` has no
 * unsupported variant, so a reverse-dependency read is answered natively or
 * emulated by the engine's bounded scan, never abandoned. The type cannot say
 * so — see the directive below.
 */
unavailable: Predicate[]
[k: string]: any
}

}
export type QueryResponseOfSearchHit = GeneratedQueryResponseOfSearchHit.QueryResponseOfSearchHit;
export namespace GeneratedRepository {
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string

}
export type Repository = GeneratedRepository.Repository;
export namespace GeneratedResolvedCredential {
/**
 * One credential name the file supplied, and where its value comes from.
 */
export interface ResolvedCredential {
/**
 * The layer whose value a plugin would receive.
 */
resolved_from: ("environment" | "secrets-file")
/**
 * The variable's name. Never its value.
 */
variable: string
[k: string]: any
}

}
export type ResolvedCredential = GeneratedResolvedCredential.ResolvedCredential;
export namespace GeneratedSearchHit {
/**
 * One hit of a search that may cross entities.
 */
export type SearchHit = (Qualified | Qualified2)

/**
 * A task matched.
 */
export interface Qualified {
kind: "task"
[k: string]: any
}
/**
 * A project matched.
 */
export interface Qualified2 {
kind: "project"
[k: string]: any
}

}
export type SearchHit = GeneratedSearchHit.SearchHit;
export namespace GeneratedSearchKind {
/**
 * Which entities a search covers.
 */
export type SearchKind = ("tasks" | "projects" | "both")

}
export type SearchKind = GeneratedSearchKind.SearchKind;
export namespace GeneratedSecretsReport {
/**
 * What the credentials file supplied, without supplying it.
 */
export interface SecretsReport {
/**
 * The file that was looked for, whether or not it was there.
 */
path?: (string | null)
/**
 * Every name the file defines, in order.
 */
variables: ResolvedCredential[]
[k: string]: any
}
/**
 * One credential name the file supplied, and where its value comes from.
 */
export interface ResolvedCredential {
/**
 * The layer whose value a plugin would receive.
 */
resolved_from: ("environment" | "secrets-file")
/**
 * The variable's name. Never its value.
 */
variable: string
[k: string]: any
}

}
export type SecretsReport = GeneratedSecretsReport.SecretsReport;
export namespace GeneratedSetting {
/**
 * One setting: what it is called, what it is set to, and where that came from.
 */
export interface Setting {
/**
 * The dotted path of the setting.
 */
key: string
/**
 * Where the value came from.
 */
origin: ({
layer: "default"
[k: string]: any
} | {
layer: "file"
/**
 * The document that set it.
 */
path: string
[k: string]: any
} | {
layer: "environment"
/**
 * The variable that set it.
 */
variable: string
[k: string]: any
} | {
/**
 * The flag that set it, as a user typed it.
 */
flag: string
layer: "flag"
[k: string]: any
})
/**
 * Its value.
 */
value: {
[k: string]: any
}
[k: string]: any
}

}
export type Setting = GeneratedSetting.Setting;
export namespace GeneratedSourceError {
/**
 * Why a source could not answer.
 * 
 * Every variant carries owned data only, so an error survives the JSON-over-stdio
 * boundary a subprocess-hosted plugin crosses without losing anything.
 */
export type SourceError = ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})

}
export type SourceError = GeneratedSourceError.SourceError;
export namespace GeneratedSourceFailure {
/**
 * One source's failure, kept beside the results the other sources returned.
 */
export interface SourceFailure {
/**
 * Why.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
/**
 * The source that failed.
 */
source: string
[k: string]: any
}

}
export type SourceFailure = GeneratedSourceFailure.SourceFailure;
export namespace GeneratedSourceListing {
/**
 * What one configured source is, as `sources list` reports it.
 */
export type SourceListing = ({
/**
 * The plugin kind behind it.
 * 
 * A `String` because the vocabulary is open, not because it was not thought about:
 * this is the kind a source reports, and a subprocess-hosted plugin reports one
 * arriving over the wire from a binary this workspace never compiled. No
 * compile-time enumeration can hold that, and a newtype over the same string would
 * only move where an unrelated value is accepted.
 */
kind: string
/**
 * The name the configuration gave it.
 */
source: string
[k: string]: any
} & ({
capabilities: Capabilities
state: "available"
[k: string]: any
} | {
/**
 * Why not.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
state: "unavailable"
[k: string]: any
}))

/**
 * What it applies itself.
 */
export interface Capabilities {
/**
 * Whether the source filters by label itself.
 */
filter_by_label: ("native" | "unsupported")
/**
 * Whether the source filters by status itself.
 */
filter_by_status: ("native" | "unsupported")
/**
 * The largest page the source will serve. At least 1 — a source that serves no rows
 * cannot be paged, and every implementation rejects zero where its config is read.
 */
max_page_size: number
/**
 * Whether the source can select tasks belonging to no project.
 */
orphan_tasks: ("native" | "unsupported")
/**
 * How far the source can walk project dependencies.
 */
project_dependencies: ("both-directions" | "forward-only")
/**
 * Whether the source has projects at all.
 */
projects: ("native" | "unsupported")
/**
 * Whether the source searches bodies itself.
 */
search_content: ("native" | "unsupported")
/**
 * Whether the source searches titles itself.
 */
search_title: ("native" | "unsupported")
/**
 * How far the source can walk task dependencies.
 */
task_dependencies: ("both-directions" | "forward-only")
[k: string]: any
}

}
export type SourceListing = GeneratedSourceListing.SourceListing;
export namespace GeneratedSourceListings {
/**
 * What one configured source is, as `sources list` reports it.
 */
export type SourceListing = ({
/**
 * The plugin kind behind it.
 * 
 * A `String` because the vocabulary is open, not because it was not thought about:
 * this is the kind a source reports, and a subprocess-hosted plugin reports one
 * arriving over the wire from a binary this workspace never compiled. No
 * compile-time enumeration can hold that, and a newtype over the same string would
 * only move where an unrelated value is accepted.
 */
kind: string
/**
 * The name the configuration gave it.
 */
source: string
[k: string]: any
} & SourceListing1)
export type SourceListing1 = ({
capabilities: Capabilities
state: "available"
[k: string]: any
} | {
/**
 * Why not.
 */
error: ({
kind: "config"
/**
 * What is wrong, in a form a user can act on.
 */
message: string
[k: string]: any
} | {
kind: "auth"
/**
 * What failed. Never contains the credential itself.
 */
message: string
[k: string]: any
} | {
kind: "refused"
/**
 * The source's own reason.
 */
message: string
[k: string]: any
} | {
kind: "rate-limited"
/**
 * How long the source asked us to wait, when it said.
 */
retry_after_seconds?: (number | null)
[k: string]: any
} | {
kind: "unavailable"
/**
 * What went wrong reaching it.
 */
message: string
[k: string]: any
} | {
kind: "malformed"
/**
 * What could not be represented.
 */
message: string
[k: string]: any
})
state: "unavailable"
[k: string]: any
})
export type SourceListings = SourceListing[]

/**
 * What it applies itself.
 */
export interface Capabilities {
/**
 * Whether the source filters by label itself.
 */
filter_by_label: ("native" | "unsupported")
/**
 * Whether the source filters by status itself.
 */
filter_by_status: ("native" | "unsupported")
/**
 * The largest page the source will serve. At least 1 — a source that serves no rows
 * cannot be paged, and every implementation rejects zero where its config is read.
 */
max_page_size: number
/**
 * Whether the source can select tasks belonging to no project.
 */
orphan_tasks: ("native" | "unsupported")
/**
 * How far the source can walk project dependencies.
 */
project_dependencies: ("both-directions" | "forward-only")
/**
 * Whether the source has projects at all.
 */
projects: ("native" | "unsupported")
/**
 * Whether the source searches bodies itself.
 */
search_content: ("native" | "unsupported")
/**
 * Whether the source searches titles itself.
 */
search_title: ("native" | "unsupported")
/**
 * How far the source can walk task dependencies.
 */
task_dependencies: ("both-directions" | "forward-only")
[k: string]: any
}

}
export type SourceListings = GeneratedSourceListings.SourceListings;
export namespace GeneratedSourcePlan {
/**
 * One thing a query can ask of a source.
 */
export type Predicate = ("label" | "status" | "search-title" | "search-content" | "project" | "reverse-dependencies")

/**
 * What one source was asked for, and what happened to each predicate.
 */
export interface SourcePlan {
/**
 * Predicates the engine applied in memory over a wider result set.
 */
applied_locally: Predicate[]
/**
 * Predicates the engine answered by a bounded scan of the source.
 */
emulated: Predicate[]
/**
 * The plugin kind behind it.
 */
kind: string
/**
 * How many pages the engine pulled from this source to answer.
 */
pages_fetched: number
/**
 * Predicates the source applied itself.
 * 
 * The four predicate vectors below partition one set of outcomes, and nothing in
 * the type says so: a `Predicate` could appear in two of them at once, or in none.
 * One `Vec<(Predicate, Outcome)>` — or a map keyed by predicate — would make that
 * unrepresentable. See the directive below for why it stays as it is.
 */
pushed_down: Predicate[]
/**
 * The configured source this describes.
 */
source: string
/**
 * Predicates neither side could answer, so the result is unconstrained.
 * 
 * Never [`Predicate::ReverseDependencies`]: `DependencySupport` has no
 * unsupported variant, so a reverse-dependency read is answered natively or
 * emulated by the engine's bounded scan, never abandoned. The type cannot say
 * so — see the directive below.
 */
unavailable: Predicate[]
[k: string]: any
}

}
export type SourcePlan = GeneratedSourcePlan.SourcePlan;
export namespace GeneratedStatus {
/**
 * A source's status, kept in both normalised and original form.
 * 
 * `category` is what every filter compares against; `name` is the source's own
 * wording, preserved so display never flattens "In Review" into "In Progress".
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}

}
export type Status = GeneratedStatus.Status;
export namespace GeneratedStatusCategory {
/**
 * The normalised status vocabulary shared across every source.
 */
export type StatusCategory = ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")

}
export type StatusCategory = GeneratedStatusCategory.StatusCategory;
export namespace GeneratedTask {
/**
 * A source's own opaque identifier for one item.
 * 
 * Deliberately unvalidated: a native id is whatever the upstream system says it
 * is, colons included. The engine parses a qualified id by splitting on the
 * *first* colon precisely so this stays true.
 */
export type NativeId = string
/**
 * A repository identified by its normalized origin, without a URL scheme or `.git` suffix.
 */
export type Repository = string

/**
 * One unit of work as a source reports it.
 */
export interface Task {
/**
 * The long-form body, when the source has one.
 */
content?: (string | null)
/**
 * When the source says the task was created.
 */
created_at?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * Inline rather than by id: a source returning a task already knows them.
 */
labels: Label[]
/**
 * Caller-defined attributes, preserving their JSON types.
 */
metadata?: {
[k: string]: any
}
/**
 * `None` is a first-class case — an orphan task — not an edge case.
 */
project?: (NativeId | null)
/**
 * Normalized repository origins this task concerns, in source order.
 */
repositories?: Repository[]
status: Status
/**
 * The one-line summary a user recognises the task by.
 */
title: string
/**
 * When the source says the task last changed.
 */
updated_at?: (string | null)
/**
 * Where a human can open this task.
 */
url?: (string | null)
[k: string]: any
}
/**
 * A tag a source attaches to work.
 */
export interface Label {
/**
 * The source's own colour for the label, when it has one.
 */
color?: (string | null)
/**
 * The source's own opaque identifier.
 */
id: string
/**
 * What a user filtering across sources actually types.
 */
name: string
[k: string]: any
}
/**
 * The source's status, normalised and preserved.
 */
export interface Status {
/**
 * The normalised value filters compare against.
 */
category: ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")
/**
 * The source's own label for this status.
 */
name: string
[k: string]: any
}

}
export type Task = GeneratedTask.Task;
export namespace GeneratedTaskQuery {
/**
 * A source's own opaque identifier for one item.
 * 
 * Deliberately unvalidated: a native id is whatever the upstream system says it
 * is, colons included. The engine parses a qualified id by splitting on the
 * *first* colon precisely so this stays true.
 */
export type NativeId = string
/**
 * The normalised status vocabulary shared across every source.
 */
export type StatusCategory = ("backlog" | "todo" | "in-progress" | "done" | "cancelled" | "unknown")

/**
 * A filter over a source's tasks.
 * 
 * Every field narrows; an empty or `None` field means unfiltered.
 */
export interface TaskQuery {
labels: LabelFilter
/**
 * Which project the task belongs to.
 */
project: ("any" | "orphans" | {
is: NativeId
})
/**
 * Status categories to keep. Empty means unfiltered.
 */
statuses: StatusCategory[]
/**
 * Free-text search, when the caller asked for one.
 */
text?: (TextQuery | null)
[k: string]: any
}
/**
 * Label membership.
 */
export interface LabelFilter {
/**
 * Keep an item carrying all of these.
 */
all_of: string[]
/**
 * Keep an item carrying at least one of these.
 */
any_of: string[]
/**
 * Drop an item carrying any of these.
 */
none_of: string[]
[k: string]: any
}
/**
 * A free-text search and the fields it searches.
 */
export interface TextQuery {
/**
 * Where to look for it.
 */
fields: ("title" | "content" | "title-or-content")
/**
 * What the user typed.
 */
terms: string
[k: string]: any
}

}
export type TaskQuery = GeneratedTaskQuery.TaskQuery;
export namespace GeneratedTextFields {
/**
 * Which fields a [`TextQuery`] searches.
 */
export type TextFields = ("title" | "content" | "title-or-content")

}
export type TextFields = GeneratedTextFields.TextFields;
